<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java1234.mapper.ChatMessageMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_chat_message (session_id, role, content, refs)
        VALUES (#{sessionId}, #{role}, #{content}, #{refs})
    </insert>

    <select id="listBySessionId" resultType="com.java1234.entity.ChatMessage">
        SELECT * FROM t_chat_message WHERE session_id = #{sessionId} ORDER BY id ASC
    </select>

    <delete id="deleteBySessionId">
        DELETE FROM t_chat_message WHERE session_id = #{sessionId}
    </delete>

    <select id="countAssistantToday" resultType="long">
        SELECT COUNT(*) FROM t_chat_message
        WHERE role = 'ASSISTANT' AND DATE(create_time) = CURDATE()
    </select>

    <select id="countAssistantByDayLast7" resultType="map">
        SELECT DATE_FORMAT(DATE(create_time), '%Y-%m-%d') AS dayKey, COUNT(*) AS cnt
        FROM t_chat_message
        WHERE role = 'ASSISTANT'
          AND create_time >= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE_FORMAT(DATE(create_time), '%Y-%m-%d')
        ORDER BY dayKey ASC
    </select>
</mapper>
